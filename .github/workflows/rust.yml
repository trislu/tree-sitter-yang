name: Rust CI

# trigger condition push/pull request to master/main
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

# workflow
jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
          
      - name: Tree-sitter setup
        # You may pin to the exact commit or the version.
        # uses: tree-sitter/setup-action@82b255ba7408ae2a2ec3f3a697ec211bff912f2a
        uses: tree-sitter/setup-action@v1
        with:
          # Install the CLI
          install-cli: # optional, default is true
          # Install the library
          install-lib: # optional, default is true
          
      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@v2

      - name: Check code formatting
        run: cargo fmt --check

      - name: Run clippy linter
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Build project
        run: cargo build --verbose

      - name: Run tests
        run: cargo test --verbose

  cross-platform-test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs: build-and-test
    strategy:
      matrix:
        os: [windows-latest, macos-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@v2

      - name: Run tests
        run: cargo test --verbose
